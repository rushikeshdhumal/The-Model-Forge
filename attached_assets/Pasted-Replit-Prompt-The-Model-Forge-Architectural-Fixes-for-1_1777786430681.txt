Replit Prompt – The Model Forge: Architectural Fixes for 11 Scenarios
Implement the following changes to correct the ML accuracy issues and architectural limitations. Do not rewrite the entire engine – only modify existing files as specified.

1. Core Engine Upgrade: Problem‑Type Metrics
File: static/scenarios.js

Add a problemType field to each scenario object. Update metric display labels dynamically.

javascript
// Example scenario structure
{
  id: "zillow",
  name: "Zillow (2021)",
  problemType: "regression",   // new field
  metrics: {
    primary: "MAE",            // Mean Absolute Error (in $1000)
    secondary: "RMSE",         // Root Mean Squared Error
    // Remove precision/recall for regression scenarios
  },
  // ... existing fields
}
In static/game.js:
When rendering metrics panel, read currentScenario.problemType and display appropriate metric names and units.

Problem Type	Metric 1 (label, unit)	Metric 2 (label, unit)
classification	Precision (%)	Recall (%)
regression	MAE ($1000)	RMSE ($1000)
ranking	NDCG (0-100)	MAP (0-100)
generative	Coherence (0-100)	Toxicity (0-100, inverted)
Default mapping for existing scenarios:

Scenario	Problem Type	Metrics
default, tay, stripe, amazon, twitter, uber, facebook	classification	Precision, Recall
zillow, tesla	regression	MAE, RMSE
netflix, google	ranking	NDCG, MAP
2. Scenario‑Specific Event Sequences
File: static/scenarios.js

Add customEvents array to each scenario. Override global event pool.

javascript
customEvents: [
  { day: 3, id: "zillow_overfit", description: "Backtest MAE = $5k, live MAE = $25k", choices: [...] },
  { day: 7, id: "market_shift", description: "Housing market volatility spikes", choices: [...] }
]
In static/game.js:
Modify triggerEvent(day) to check currentScenario.customEvents first. If none for that day, fall back to global event pool.

Create event definitions for each unique failure mode (see section 3).

3. Per‑Scenario Event Fixes (by ID)
Replace or add these event objects in static/events.js (or inline in scenario customEvents).

Zillow (regression)
javascript
{
  id: "zillow_overfit",
  title: "Backtest vs. Live MAE Divergence",
  description: "Your model predicts home prices with MAE $5k in backtest, but live MAE is $25k. Overfitting to historical market patterns.",
  choices: [
    { text: "Apply regularization (ridge regression)", effect: { mae: -10, cost: +5 }, concept: "Regularization reduces overfitting" },
    { text: "Collect more recent transaction data", effect: { mae: -15, cost: +10, delay: 2 }, concept: "Data freshness matters" },
    { text: "Ignore – market will stabilize", effect: { mae: +5 }, concept: "Overfitting worsens" }
  ]
}
Microsoft Tay (generative – modify problemType to "generative")
javascript
{
  id: "tay_poison",
  title: "Chatbot Repeats Offensive User Input",
  description: "Model is learning in real-time from Twitter replies. It now generates harmful content.",
  choices: [
    { text: "Add input content filter (regex + blocklist)", effect: { toxicity: -20, cost: +5 }, concept: "Input validation" },
    { text: "Disable online learning, retrain offline hourly", effect: { toxicity: -30, coherence: -5, cost: +15 }, concept: "Offline retraining tradeoff" },
    { text: "Do nothing", effect: { toxicity: +15 }, concept: "Poisoning accelerates" }
  ]
}
Amazon (classification – keep, but fix fairness framing)
javascript
{
  id: "amazon_bias",
  title: "Demographic Disparity Detected",
  description: "Model gives lower scores to résumés with women-associated terms. Disparate impact ratio = 0.6.",
  choices: [
    { text: "Reweight training data to balance labels", effect: { fairness: +25, recall: -2 }, concept: "Data reweighting" },
    { text: "Add adversarial debiasing to training objective", effect: { fairness: +30, cost: +15 }, concept: "Adversarial debiasing" },
    { text: "Ignore – it's just historical data", effect: { compliance: -30 }, concept: "Regulatory intervention" }
  ]
}
Add fairness metric (0-100) for bias scenarios. Display only when relevant.

Twitter (ranking – change problemType to "ranking")
javascript
{
  id: "twitter_amplification",
  title: "Political Amplification Disparity",
  description: "Algorithm favors one political party's content by 18% in feed engagement. Engagement optimization causes unintended skew.",
  choices: [
    { text: "Add diversity penalty to ranking objective", effect: { ndcg: -3, fairness: +20 }, concept: "Multi‑objective optimization" },
    { text: "Audit training data for label bias", effect: { fairness: +15, cost: +8 }, concept: "Data audit" },
    { text: "Keep maximizing engagement", effect: { fairness: -10, engagement: +5 }, concept: "Short‑term gain, long‑term risk" }
  ]
}
Uber (classification – fix cost multiplier and model type)
Ensure Uber scenario overrides registry.models[0].type = "Neural Network" in initial state.
Then event:

javascript
{
  id: "uber_latency",
  title: "Traffic Spike Causes Inference Latency Crisis",
  description: "P99 latency hit 180ms (SLA = 100ms). Surge pricing predictions delayed.",
  choices: [
    { text: "Fall back to XGBoost (simpler model)", effect: { latency: -60, precision: -5 }, concept: "Accuracy vs. speed tradeoff" },
    { text: "Scale inference cluster (2x replicas)", effect: { latency: -40, cost: +100 }, concept: "Horizontal scaling" },  // +100% of original cost
    { text: "Do nothing – hope traffic drops", effect: { slaAdherence: -20 }, concept: "SLA violation penalties" }
  ]
}
Fix cost effect: inferenceCost = inferenceCost * 2 (not +40).

Netflix (ranking – differentiate from Google)
javascript
{
  id: "netflix_drift_gradual",
  title: "Gradual Concept Drift (COVID Lockdowns)",
  description: "Viewing behavior shifted from commute/office hours to binge watching. Recommendation quality declining slowly.",
  choices: [
    { text: "Enable auto‑retraining daily (CI/CD)", effect: { ndcg: +2, cost: +5 }, concept: "Continuous retraining" },
    { text: "Manual retrain with last 7 days of data", effect: { ndcg: +3, cost: +3, delay: 1 }, concept: "Triggered retraining" },
    { text: "Ignore – ride it out", effect: { ndcg: -2 }, concept: "Drift accumulates" }
  ]
}
Google (ranking – sudden drift)
javascript
{
  id: "google_drift_sudden",
  title: "LLM‑Generated Content Flood",
  description: "Overnight, 40% of search results are AI‑generated. Pre‑LLM spam signals are now non‑discriminative.",
  choices: [
    { text: "Train new feature extractor on LLM content", effect: { ndcg: +8, cost: +30 }, concept: "Feature adaptation" },
    { text: "Use ensemble with LLM detector", effect: { ndcg: +5, cost: +15 }, concept: "Ensemble for distribution shift" },
    { text: "Keep existing pipeline", effect: { ndcg: -10 }, concept: "Sudden drift kills performance" }
  ]
}
4. Fix Documentation/Code Mismatches
File: static/scenarios.js – Correct starting handicap values to match briefs.

Scenario	Brief value	Code value (fix to brief)
Tesla	Recall 72%	75% → 72%
Uber	SLA 92%	91% → 92%
Facebook	SLA 90%	91% → 90%
File: static/game.js – In advanceDay(), when ciCd.autoRetrain is true, add cost increment:

javascript
if (state.ciCd.autoRetrain) {
  state.metrics.inferenceCost += 2;  // $0.02 per 1k predictions
}
Update Codex text to reflect actual cost.

5. UI Additions for Clarity
Scenario selection card – Add a badge showing problem type (e.g., “Regression”, “Ranking”).

Metric tooltips – On hover, display formula and real‑world interpretation. For MAE: “Mean Absolute Error – average $ error per prediction.”

Event differentiation – Color‑code event cards by scenario type (orange for overfitting, red for poisoning, blue for drift, purple for bias, yellow for latency).

Implementation Order
Add problemType and update metric labels.

Create custom event objects for each of the 11 scenarios (focus on the 6 incorrect ones: Zillow, Tay, Amazon, Twitter, Uber, Netflix, Google).

Modify event trigger logic to use scenario‑specific events.

Fix data mismatches.

Update UI badges and tooltips.

Test each scenario – verify unique events fire and metrics behave correctly.

Final check: After implementation, a player playing Zillow should see MAE, not Precision/Recall. Tay should have toxicity/coherence metrics. Each scenario’s first event should be unique to that failure mode.

Build these fixes. The Model Forge will finally earn its name.